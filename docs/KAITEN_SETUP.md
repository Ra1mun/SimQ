# Kaiten (Wekan) - Управление проектами

## 🎯 Описание

Kaiten - это open-source система управления проектами в стиле Kanban, аналог Trello и Jira. 
Мы используем Wekan как основу для замены Jira в проекте SimQ.

## 🌐 Доступ к сервису

- **URL**: http://localhost:8090
- При первом запуске необходимо создать учетную запись администратора

## 📦 Компоненты

### Kaiten (Wekan)
- **Контейнер**: `kaiten`
- **Порт**: 8090
- **База данных**: MongoDB (используется общая с SimQ Backend)

### База данных Kaiten
- **Контейнер**: `kaiten-db`
- **Тип**: PostgreSQL 15
- **База**: kaiten (резервная, основная - MongoDB)

## 🚀 Первый запуск

### 1. Откройте Kaiten
Перейдите по адресу: http://localhost:8090

### 2. Создайте учетную запись администратора
При первом входе Wekan предложит создать учетную запись:
1. Введите email
2. Придумайте имя пользователя
3. Установите пароль
4. Нажмите "Register"

### 3. Создайте первую доску
1. Нажмите "Create Board"
2. Введите название доски
3. Выберите цвет
4. Нажмите "Create"

## ✨ Основные возможности

### Доски (Boards)
- Создание неограниченного количества досок
- Настройка цветов и фонов
- Архивирование досок

### Списки (Lists)
- Создание колонок для задач
- Перемещение списков
- Лимиты Work In Progress (WIP)

### Карточки (Cards)
- Описание задач
- Чек-листы
- Метки (labels)
- Участники
- Сроки выполнения
- Вложения
- Комментарии

### Участники команды
- Приглашение пользователей
- Назначение ролей (Admin, Member, Observer)
- Управление доступом к доскам

### Интеграция
- REST API для автоматизации
- Webhooks для интеграции с другими системами

## 🔧 Настройка

### Изменить пароль базы данных

Отредактируйте файл `.env`:
```env
KAITEN_DB_PASSWORD=ваш_безопасный_пароль
```

Перезапустите сервисы:
```bash
docker-compose restart kaiten-db kaiten
```

### Настроить email (опционально)

Для отправки уведомлений по email, измените в `docker-compose.yml`:
```yaml
environment:
  - MAIL_URL=smtp://ваш_пользователь:пароль@smtp.gmail.com:587/
  - MAIL_FROM=noreply@yourdomain.com
```

### Включить/выключить регистрацию

По умолчанию регистрация открыта. Чтобы её отключить после создания учетных записей:
1. Войдите как администратор
2. Settings → Registration → Disable Registration

## 📊 Интеграция с SimQ

### API доступ

Kaiten (Wekan) предоставляет REST API для интеграции:
- Документация API: http://localhost:8090/api/docs

### Создание задач из SimQ

Вы можете автоматически создавать карточки в Kaiten при создании задач моделирования в SimQ:

1. Получите API token в настройках Kaiten
2. Используйте API для создания карточек:

```bash
curl -X POST http://localhost:8090/api/boards/{boardId}/lists/{listId}/cards \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "title": "Задача моделирования #123",
    "description": "Описание задачи"
  }'
```

## 🛠️ Управление

### Резервное копирование данных

Данные Kaiten хранятся в MongoDB. Для резервного копирования:

```bash
# Экспорт базы данных wekan
docker exec simq-mongodb mongodump --db wekan --out /dump

# Копирование дампа на хост
docker cp simq-mongodb:/dump ./backup/wekan-dump
```

### Восстановление данных

```bash
# Копирование дампа в контейнер
docker cp ./backup/wekan-dump simq-mongodb:/dump

# Восстановление базы данных
docker exec simq-mongodb mongorestore --db wekan /dump/wekan
```

### Обновление версии

```bash
# Остановить контейнер
docker-compose stop kaiten

# Обновить образ
docker-compose pull kaiten

# Запустить заново
docker-compose up -d kaiten
```

## 🔍 Устранение проблем

### Kaiten не запускается

1. Проверьте логи:
   ```bash
   docker logs kaiten
   ```

2. Проверьте, что MongoDB запущена:
   ```bash
   docker-compose ps | Select-String mongodb
   ```

3. Перезапустите контейнер:
   ```bash
   docker-compose restart kaiten
   ```

### Не могу войти

1. Проверьте URL: http://localhost:8090
2. Если забыли пароль, можно сбросить через MongoDB:
   ```bash
   docker exec -it simq-mongodb mongosh wekan
   db.users.find()
   ```

### Медленная работа

Kaiten (Wekan) может работать медленно на слабых машинах. Рекомендации:
- Минимум 2GB RAM для контейнера
- SSD диск для базы данных
- Отключите ненужные плагины

## 📚 Дополнительная информация

### Официальная документация Wekan
- Сайт: https://wekan.github.io/
- GitHub: https://github.com/wekan/wekan
- API документация: https://github.com/wekan/wekan/wiki/REST-API

### Сравнение с Jira

| Функция | Kaiten (Wekan) | Jira |
|---------|----------------|------|
| Kanban доски | ✅ | ✅ |
| Scrum доски | ⚠️ Базовая | ✅ |
| Gantt диаграммы | ❌ | ✅ |
| Отчеты | ⚠️ Базовые | ✅ |
| Стоимость | 🆓 Free | 💰 Платная |
| Self-hosted | ✅ | ⚠️ Ограниченно |
| Открытый код | ✅ | ❌ |

### Альтернативы

Если Wekan не подходит, рассмотрите:
- **Taiga** - более продвинутая альтернатива
- **OpenProject** - полнофункциональная система управления проектами
- **Redmine** - классическая система управления проектами

## ⚙️ Конфигурация в docker-compose.yml

```yaml
kaiten:
  image: wekanteam/wekan:latest
  container_name: kaiten
  restart: unless-stopped
  ports:
    - "8090:8080"
  environment:
    - MONGO_URL=mongodb://admin:password@mongodb:27017/wekan?authSource=admin
    - ROOT_URL=http://localhost:8090
    - WITH_API=true
  networks:
    - simq-network
  depends_on:
    - mongodb
```

## 🎓 Рекомендации

1. **Создайте отдельные доски** для разных проектов SimQ
2. **Используйте метки** для категоризации задач
3. **Настройте автоматизацию** через API
4. **Регулярно делайте резервные копии** базы данных
5. **Ограничьте регистрацию** после создания всех учетных записей

---

**Удачи в управлении проектами! 🚀**
