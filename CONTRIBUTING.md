# Руководство для контрибьюторов

Спасибо за интерес к проекту SimQ! Это руководство поможет вам начать вносить вклад в проект.

## Оглавление

- [Кодекс поведения](#кодекс-поведения)
- [Как внести вклад](#как-внести-вклад)
- [Процесс разработки](#процесс-разработки)
- [Стандарты кода](#стандарты-кода)
- [Тестирование](#тестирование)
- [Документация](#документация)
- [Сообщения о багах](#сообщения-о-багах)
- [Предложения новых функций](#предложения-новых-функций)

## Кодекс поведения

Участвуя в проекте, вы соглашаетесь следовать нашему кодексу поведения:

- Будьте уважительны к другим участникам
- Принимайте конструктивную критику
- Фокусируйтесь на том, что лучше для сообщества
- Проявляйте эмпатию к другим участникам

## Как внести вклад

### Первые шаги

1. **Fork репозитория**
   ```bash
   # Нажмите кнопку "Fork" на GitHub
   ```

2. **Клонируйте свой fork**
   ```bash
   git clone https://github.com/YOUR-USERNAME/SimQ.git
   cd SimQ
   ```

3. **Добавьте upstream remote**
   ```bash
   git remote add upstream https://github.com/Ra1mun/SimQ.git
   ```

4. **Настройте окружение**
   ```bash
   # Backend
   cd SimQ.WebApi
   dotnet restore
   
   # Frontend
   cd client
   npm install
   ```

### Создание изменений

1. **Создайте новую ветку**
   ```bash
   git checkout -b feature/my-new-feature
   # или
   git checkout -b fix/issue-123
   ```

2. **Внесите изменения**
   - Пишите чистый, читаемый код
   - Следуйте стандартам проекта
   - Добавьте тесты для новой функциональности

3. **Закоммитьте изменения**
   ```bash
   git add .
   git commit -m "feat: add new feature"
   ```
   
   Используйте [Conventional Commits](#формат-коммитов) для сообщений.

4. **Синхронизируйтесь с upstream**
   ```bash
   git fetch upstream
   git rebase upstream/main
   ```

5. **Push в свой fork**
   ```bash
   git push origin feature/my-new-feature
   ```

6. **Создайте Pull Request**
   - Перейдите на GitHub
   - Нажмите "New Pull Request"
   - Выберите вашу ветку
   - Заполните описание PR

## Процесс разработки

### Формат коммитов

Мы используем [Conventional Commits](https://www.conventionalcommits.org/):

```
<type>(<scope>): <subject>

<body>

<footer>
```

**Типы**:
- `feat:` — новая функциональность
- `fix:` — исправление бага
- `docs:` — изменения в документации
- `style:` — форматирование, отсутствующие точки с запятой и т.д.
- `refactor:` — рефакторинг кода
- `test:` — добавление тестов
- `chore:` — обновление задач сборки, настройки и т.д.
- `perf:` — улучшение производительности

**Примеры**:
```bash
feat(core): add support for priority queues
fix(api): resolve null reference in ProblemService
docs(readme): update installation instructions
refactor(modeller): simplify event processing logic
test(core): add unit tests for AgentFactory
```

### Ветвление

- `main` — стабильная ветка, готова к продакшену
- `develop` — ветка разработки
- `feature/*` — новые функции
- `fix/*` — исправления багов
- `hotfix/*` — критические исправления для продакшена

### Pull Request процесс

1. **Создайте PR** с описательным названием
2. **Заполните шаблон PR**:
   - Что изменилось
   - Почему это нужно
   - Как протестировать
   - Скриншоты (если применимо)
3. **Пройдите code review**
4. **Исправьте замечания** ревьюеров
5. **Дождитесь одобрения** и merge

### Code Review критерии

Ревьюеры проверяют:

- ✅ Код соответствует стандартам проекта
- ✅ Все тесты проходят
- ✅ Нет breaking changes без документации
- ✅ Код покрыт тестами
- ✅ Документация обновлена (если нужно)
- ✅ Нет конфликтов с main веткой
- ✅ Производительность не ухудшилась

## Стандарты кода

### C# (.NET)

**Именование**:
```csharp
// PascalCase для публичных членов
public class ProblemService { }
public int MaxCapacity { get; set; }
public void ProcessEvent() { }

// camelCase для приватных полей с префиксом _
private readonly IProblemRepository _repository;
private int _counter;

// camelCase для параметров и локальных переменных
public void Method(int parameterName)
{
    var localVariable = 0;
}
```

**Форматирование**:
```csharp
// Открывающая скобка на новой строке
public class MyClass
{
    public void MyMethod()
    {
        if (condition)
        {
            // код
        }
    }
}

// Используйте var когда тип очевиден
var list = new List<string>();
IProblemService service = new ProblemService();

// Async методы имеют суффикс Async
public async Task<Result> GetResultAsync(CancellationToken ct)
{
    return await _repository.GetAsync(ct);
}
```

**LINQ**:
```csharp
// Предпочитайте LINQ методы
var activeAgents = agents
    .Where(a => a.IsActive)
    .OrderBy(a => a.Priority)
    .ToList();

// Вместо циклов
foreach (var agent in agents)
{
    if (agent.IsActive)
        activeList.Add(agent);
}
```

### TypeScript/React

**Именование**:
```typescript
// PascalCase для компонентов и типов
interface ProblemData { }
type AgentType = 'Source' | 'Service';

function ProblemCard(props: ProblemCardProps) { }

// camelCase для переменных и функций
const problemList: ProblemData[] = [];
function fetchProblems() { }
```

**Компоненты**:
```typescript
// Используйте функциональные компоненты с хуками
import { useState, useEffect } from 'react';

interface ProblemCardProps {
  problem: Problem;
  onSelect: (id: string) => void;
}

export function ProblemCard({ problem, onSelect }: ProblemCardProps) {
  const [selected, setSelected] = useState(false);
  
  useEffect(() => {
    // эффект
  }, [problem.id]);
  
  return (
    <div onClick={() => onSelect(problem.id)}>
      {problem.name}
    </div>
  );
}
```

**Типизация**:
```typescript
// Всегда типизируйте пропсы и состояние
interface State {
  problems: Problem[];
  loading: boolean;
}

// Избегайте any
// ❌ Плохо
function process(data: any) { }

// ✅ Хорошо
function process(data: Problem) { }
```

### SQL/MongoDB

**Запросы**:
```javascript
// Используйте проекции для ограничения полей
db.Problems.find(
  { status: "active" },
  { name: 1, createdAt: 1, _id: 0 }
);

// Используйте индексы
db.Problems.createIndex({ "createdAt": -1 });

// Лимитируйте результаты
db.Problems.find().limit(100);
```

## Тестирование

### Unit тесты

**Структура теста**:
```csharp
public class ProblemServiceTests
{
    // Arrange - подготовка
    private readonly Mock<IProblemRepository> _repositoryMock;
    private readonly IProblemService _service;
    
    public ProblemServiceTests()
    {
        _repositoryMock = new Mock<IProblemRepository>();
        _service = new ProblemService(_repositoryMock.Object);
    }
    
    [Fact]
    public async Task GetProblemAsync_ValidId_ReturnsProblem()
    {
        // Arrange
        var expectedProblem = new ProblemEntity { Id = "123" };
        _repositoryMock
            .Setup(r => r.GetByIdAsync("123", default))
            .ReturnsAsync(expectedProblem);
        
        // Act
        var result = await _service.GetProblemAsync("123", default);
        
        // Assert
        Assert.NotNull(result);
        Assert.Equal("123", result.Id);
    }
}
```

**Запуск тестов**:
```bash
# Все тесты
dotnet test

# Конкретный класс
dotnet test --filter "FullyQualifiedName~ProblemServiceTests"

# С coverage
dotnet test --collect:"XPlat Code Coverage"
```

### Интеграционные тесты

```csharp
public class ProblemsControllerIntegrationTests : IClassFixture<WebApplicationFactory<Program>>
{
    private readonly WebApplicationFactory<Program> _factory;
    
    [Fact]
    public async Task GetProblems_ReturnsSuccessStatusCode()
    {
        // Arrange
        var client = _factory.CreateClient();
        
        // Act
        var response = await client.GetAsync("/problems/v1/problems");
        
        // Assert
        response.EnsureSuccessStatusCode();
    }
}
```

### Тестовое покрытие

Стремитесь к покрытию:
- **Unit тесты**: минимум 80% для бизнес-логики
- **Интеграционные тесты**: основные сценарии API
- **E2E тесты**: критические пользовательские сценарии

## Документация

### Комментарии в коде

**XML документация для публичного API**:
```csharp
/// <summary>
/// Получает задачу моделирования по идентификатору.
/// </summary>
/// <param name="problemId">Идентификатор задачи.</param>
/// <param name="cancellationToken">Токен отмены операции.</param>
/// <returns>Задача моделирования или null, если не найдена.</returns>
/// <exception cref="ArgumentNullException">Если problemId равен null.</exception>
public async Task<ProblemResponse?> GetProblemAsync(
    string problemId, 
    CancellationToken cancellationToken = default)
{
    // реализация
}
```

**JSDoc для TypeScript**:
```typescript
/**
 * Fetches problem data from the API.
 * @param problemId - The unique identifier of the problem
 * @returns Promise resolving to problem data
 * @throws {Error} If the problem is not found
 */
async function fetchProblem(problemId: string): Promise<Problem> {
  // реализация
}
```

### README и документы

При добавлении новой функциональности обновите:

- `README.md` — если изменился процесс установки или использования
- `docs/API.md` — если добавлены новые эндпоинты
- `docs/DEVELOPMENT.md` — если изменился процесс разработки
- `docs/ARCHITECTURE.md` — если изменилась архитектура

## Сообщения о багах

### Перед созданием issue

1. Проверьте, что баг не был уже [зарепорчен](https://github.com/Ra1mun/SimQ/issues)
2. Убедитесь, что это действительно баг, а не вопрос использования
3. Проверьте на последней версии `main` ветки

### Шаблон bug report

```markdown
**Описание бага**
Четкое описание того, что происходит.

**Шаги для воспроизведения**
1. Перейти на '...'
2. Кликнуть на '...'
3. Прокрутить до '...'
4. Увидеть ошибку

**Ожидаемое поведение**
Что должно было произойти.

**Скриншоты**
Если применимо, добавьте скриншоты.

**Окружение:**
 - OS: [e.g. Windows 11]
 - Browser: [e.g. Chrome 120]
 - Version: [e.g. 1.0.0]

**Дополнительный контекст**
Любая дополнительная информация о проблеме.

**Логи**
```
Вставьте логи здесь
```
```

## Предложения новых функций

### Шаблон feature request

```markdown
**Связана ли ваша feature request с проблемой? Опишите.**
Четкое описание проблемы. Например: "Я всегда расстраиваюсь когда [...]"

**Опишите решение, которое вы хотели бы**
Четкое описание того, что вы хотите видеть.

**Опишите альтернативы, которые вы рассматривали**
Описание альтернативных решений или функций.

**Дополнительный контекст**
Добавьте любой другой контекст или скриншоты о feature request.
```

### Процесс добавления функции

1. **Создайте issue** с описанием функции
2. **Дождитесь обсуждения** с мейнтейнерами
3. **Получите одобрение** перед началом работы
4. **Создайте PR** со ссылкой на issue

## Полезные ресурсы

### Документация проекта

- [README](../README.md) — общее описание
- [DEVELOPMENT](DEVELOPMENT.md) — руководство разработчика
- [API](API.md) — документация API
- [ARCHITECTURE](ARCHITECTURE.md) — архитектура системы
- [DEPLOYMENT](DEPLOYMENT.md) — развертывание

### Внешние ресурсы

- [ASP.NET Core Docs](https://docs.microsoft.com/aspnet/core)
- [React Docs](https://react.dev)
- [MongoDB .NET Driver](https://mongodb.github.io/mongo-csharp-driver/)
- [Conventional Commits](https://www.conventionalcommits.org/)

## Вопросы?

Если у вас есть вопросы:

1. Проверьте [документацию](../README.md)
2. Поищите в существующих [issues](https://github.com/Ra1mun/SimQ/issues)
3. Создайте новый issue с меткой "question"

## Благодарности

Спасибо всем, кто вносит вклад в проект! 🎉

Список контрибьюторов можно увидеть на [странице contributors](https://github.com/Ra1mun/SimQ/graphs/contributors).

---

Спасибо за то, что делаете SimQ лучше!
